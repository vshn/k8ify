services:
  myservice:
    image: nginx
    volumes:
      - default-data:/data
  myinitcontainer:
    image: initcontainer
    labels:
      k8ify.initContainerOf: myservice
    volumes:
      - default-data:/data

volumes:
  default-data: {}
