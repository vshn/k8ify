apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    k8ify.ref-slug: oasp
    k8ify.volume: sessions
  name: sessions-oasp
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
status: {}
